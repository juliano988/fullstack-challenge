
export type Book = {
  title: string,
  subtitle: string,
  author: string,
  description: string,
  cover: string,
  fake: boolean
}

export type PageMeta = {
  page: number,
  pages: number,
  booksInPage: number,
  books: number
}